

// function SectionTitle(props) {
//   const tagStyle = {
    
//   }
//   const Tag = props.tag
//   return (
//     <>

//     <Tag> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </Tag>

//     {/* { props.tag==='h1' ? <h1> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </h1>
//       : (
//         props.tag==='h2' ? <h2> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </h2>
//         : (
//           props.tag==='h3' ? <h3> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </h3>
//           : (
//             props.tag==='h4' ? <h4> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </h4>
//             : (
//               props.tag==='p' ? <p> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </p>
//               : 
//               <h4> {props.showSection ? (props.showTitleInSection ? props.title : '') : props.title} </h4>
//       ))))} */}
//     </>
//   )
// }

// export default SectionTitle 